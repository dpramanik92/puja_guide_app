# Copy this to .env and fill in real values before running docker-compose
# These are read by docker-compose.yml and injected into the backend container.

OPENAI_API_KEY=sk-...
TAVILY_API_KEY=tvly-...
